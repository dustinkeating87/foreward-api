[CLAUDE.md](https://github.com/user-attachments/files/27259697/CLAUDE.md)
# Good Lie Golf — frontend repo (`foreward`)

This repo is the **Lovable-generated React/TS frontend** for Good Lie Golf, a tee-time alert service for GTA-area golf courses. Production URL: https://goodlie.golf/

## Read this before any work

The canonical architecture, schema, and decision log for the whole Good Lie Golf system lives at:

**`~/Documents/Claude/Projects/Good Lie Golf/ARCHITECTURE.md`**

(Adjust the home directory to match wherever Dustin's workspace folder lives on the machine you're running on. If unreachable, ask Dustin for the latest copy.)

ClickUp space `Good Lie Golf` (id `901313780791`) is the source of truth for open work — query it for current tasks, priorities, blockers.

## What lives in this repo

- React/TS application managed by Lovable
- Pages: `/`, `/signup`, `/login`, `/dashboard`, `/alerts/*`, `/billing`, `/admin`, `/courses/request`
- Lovable bot pushes commits to `main`; you can also push manually
- Single edge function: `course-request` (writes to Supabase `course_requests` table)

## What does NOT live here

- API logic — that's in `foreward-api`
- Scraper / SMS sending / alert matching — that's in `foreward-scraper`
- Database schema — Supabase project `offtdltmvjfizkoeywei`

If a request involves SMS, scraping, alert matching, or schema changes, it does NOT belong in this repo. Confirm before making code changes here.

## Naming rule

Never use the word "snipe" in user-facing copy. Use "alert," "match," or "opening." Internal-only term.

## Common pitfalls

- Bun lockfile (`bun.lock`, `bun.lockb`) AND npm lockfile (`package-lock.json`) both exist. Confirm which package manager Lovable uses before installing.
- This repo's code is regenerated by Lovable AI prompts. Hand-edits can be overwritten — prefer Lovable prompts for substantive changes.
